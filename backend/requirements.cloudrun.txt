fastapi>=0.141.1
uvicorn[standard]>=0.52.4
python-dotenv>=1.2.3
sse-starlette>=3.4.8
aiosqlite>=0.22.1
# Postgres backend for the job store — required for durable storage on Cloud
# Run, where the SQLite fallback lives in /tmp and is wiped on every cold start.
psycopg[binary,pool]>=3.2.0
httpx>=0.28.1
# Fernet encryption for OAuth session tokens at rest (see SESSION_TOKEN_KEY).
cryptography>=43.0.0
# repo-people is the data engine — pinned to an exact PyPI release so Cloud Run
# and CI build the same version as local/Vercel. Bump deliberately (Dependabot).
repo-people==1.0.2
