fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
sse-starlette>=2.1.0
aiosqlite>=0.19.0
httpx>=0.28.1
# repo-people is the data engine (role fetchers + profile lookups in worker.py).
# Pinned to an exact PyPI release for reproducible builds across every deploy
# target (local, Vercel, Cloud Run, CI). Bump deliberately — Dependabot opens a
# PR when a new release is published (.github/dependabot.yml).
repo-people==1.0.2