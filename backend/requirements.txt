fastapi>=0.141.1
uvicorn[standard]>=0.52.4
python-dotenv>=1.2.2
sse-starlette>=3.4.8
aiosqlite>=0.22.1
# Postgres backend for the job store. Only imported when DATABASE_URL is set;
# without it the app falls back to SQLite (see backend/store.py).
psycopg[binary,pool]>=3.2.0
httpx>=0.28.1
# Fernet encryption for OAuth session tokens at rest (see SESSION_TOKEN_KEY).
cryptography>=43.0.0
# repo-people is the data engine (role fetchers + profile lookups in worker.py).
# Pinned to an exact PyPI release for reproducible builds across every deploy
# target (local, Vercel, Cloud Run, CI). Bump deliberately — Dependabot opens a
# PR when a new release is published (.github/dependabot.yml).
repo-people==1.0.2